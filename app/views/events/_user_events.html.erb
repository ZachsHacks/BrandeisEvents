<div id="user_events">
    <ul class="nav nav-tabs">
        <% set_active %>

        <% @tab_titles = ['RSVP\'d events', 'Interested Events', 'Events For You'] %>

        <% for i in 0..2 %>
            <%= render partial: 'events/event_tab', locals: {index: i} %>
        <% end %>
    </ul>

    <div class="tab-content">

        <% @page_titles = ['Events you\'re going to', 'Events you\'re interested in', 'Recommended events for you'] %>

        <% for i in 0..2 %>
            <%= render partial: 'events/event_section', locals: {index: i} %>
        <% end %>

    </div>

</div>

<style>
    .nav-pills > li,
    .nav-tabs > li {
        float: none;
        display: inline-block;
        *display: inline;
        /* ie7 fix */
        zoom: 1;
        /* hasLayout ie7 trigger */
    }

    .nav-pills,
    .nav-tabs {
        text-align: center;
    }


</style>
